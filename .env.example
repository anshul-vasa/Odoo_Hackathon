# Only needed for production (e.g. Netlify) or to point local dev at a real
# Turso database instead of the default local data/transitops.db file.
# Leave both unset for local dev — no account needed.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
